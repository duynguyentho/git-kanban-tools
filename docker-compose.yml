version: "3.7"

services:
  app:
    image: git-kanban/node:v2
    build: .
    command: npm run start
    working_dir: /app
    ports:
      - 80:3000
    volumes:
      - .:/app
    restart: unless-stopped
